import { FilterGenrePipe } from './filter-genre.pipe';

describe('FilterGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
